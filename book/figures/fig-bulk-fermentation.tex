\begin{tikzpicture}[node distance = 3cm, auto]
  \node [start] (init) {Stockgare};
  \node [block, right of=init, node distance=4cm] (check_dough) {Teig prüfen};
  \node [block, right of=check_dough, node distance=4cm] (size_increase) {Größen\-zunahme prüfen};
  \node [block, below of=size_increase, node distance=2cm] (ph_value) {pH-Wert des Teigs prüfen};
  \node [block, below of=ph_value, node distance=2cm] (smell) {Geruch des Teigs prüfen};
  \node [decision, right of=size_increase, node distance=4cm] (dough_ready) {Teig fertig?};
  \node [success] at(dough_ready |- smell) (divide_preshape) {Teilen und vorformen};
  \node [decision, above of=size_increase] (dough_flattened) {Teig flach geworden?};
  \node [block, above of=check_dough] (wait_60_minutes) {60~Minuten warten};
  \node [block, above of=wait_60_minutes] (stretch_fold) {Dehnen und falten};

  \path [line] (init) -- (check_dough);
  \path [line] (check_dough) -- (size_increase);
  % Tricks not to get double lines
  \path [line] (check_dough) ++(2, -2) -- node{oder} (ph_value);
  \path [line] (check_dough) ++(2, 0) -- node{} ++(0, -4) -- node{oder} (smell);
  \path [line] (check_dough) ++(2, -4) -- node{oder} (smell);
  \path [line] (size_increase) -- (dough_ready);
  % Same tricks not to get double lines and also we do _not_ want arrows
  \path [draw, thick] (ph_value) -- node{} ++(2, 0);
  \path [draw, thick] (smell) -| node{} ++(2, 4);
  \path [line] (dough_ready) -- node{Ja} (divide_preshape);
  \path [line] (dough_ready) |- node[right=3pt]{Nein} (dough_flattened);
  \path [line] (dough_flattened) |- node[right=3pt]{Ja} (stretch_fold);
  \path [line] (dough_flattened) -- node{Nein} (wait_60_minutes);
  \path [line] (stretch_fold) -- (wait_60_minutes);
  \path [line] (wait_60_minutes) -- (check_dough);
\end{tikzpicture}
